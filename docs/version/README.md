# 更新日志

## v2.10.0
```待发布```
* 新增 K8s 服务树资源绑定与权限管理模型
* 新增服务树、资源清单、自动绑定规则、资源绑定关系表
* 新增服务树授权策略、授权主体、授权节点、资源子集过滤、动作权限表
* 新增服务树查询、节点创建、资源绑定、未归类资源、绑定规则、授权策略接口
* 工作负载列表支持可选 `treeNodeId` 过滤，兼容原 namespace 查询
* 前端工作负载页新增 namespace -> 服务 -> 环境/集群 服务树入口

SQL
```SQL
本次版本变更SQL如下
docs/sql/v2.10.0.sql

回滚SQL如下
docs/sql/v2.10.0-rollback.sql
```

设计说明
```text
docs/k8s-service-tree-rbac.md
```
